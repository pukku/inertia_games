package stalbans.inertiagames;import java.awt.*;import javax.swing.*;public class SpeedometerPanel extends RocketPanel {		protected JLabel speedometer;		// the label which we display the speed in		public SpeedometerPanel(RocketPanelCreator r) {		super(r);	}		public void setSpeedometer(JLabel s) {		speedometer = s;	}		public JLabel getSpeedometer() {		return speedometer;	}		protected void paintGameScreen(Graphics g) {		double tempval = theRocket.getSpeed();		tempval = ((double)Math.round(tempval * 100)) / 100;		speedometer.setText("Speed: " + (Double.toString(tempval)));	}		public boolean gameOver() {		return false;	}	}