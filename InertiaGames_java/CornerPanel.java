package stalbans.inertiagames;import java.awt.*;import java.lang.Math;public class CornerPanel extends RocketPanel {	private int CORNER_X;	private int CORNER_Y;	private int FINISH_Y = 10;		public CornerPanel(RocketPanelCreator r) {		super(r);	}		public void setCorner(int x, int y) {		CORNER_X = x;		CORNER_Y = y;	}	protected void paintGameScreen(Graphics g) {		g.setColor(java.awt.Color.gray);		g.fillRect(0,0,CORNER_X,CORNER_Y);		g.setColor(java.awt.Color.black);		g.drawLine(CORNER_X, 0, CORNER_X, CORNER_Y);		g.drawLine(0, CORNER_Y, CORNER_X, CORNER_Y);		g.drawLine(CORNER_X, FINISH_Y, getWidth(), FINISH_Y);	}		public boolean gameOver() {			// game is over if we are above the finish line		if (((getHeight() - theRocket.gety()) < FINISH_Y) &&			// the rocket is in the leg...			(theRocket.getx() > CORNER_X) &&			(theRocket.getx() < getWidth()))				{ return true; }		else 	{ return false; }	}		public boolean crashed() {			// the rocket has crashed if it has gone off the screen		if ((theRocket.gety() < 0) ||			(theRocket.getx() < 0) ||			(theRocket.getx() > getWidth()) ||			// or if it is in the square area			((theRocket.getx() < CORNER_X) && ((getHeight() - theRocket.gety()) < CORNER_Y)))				{ return true; }		else	{ return false; }	}}